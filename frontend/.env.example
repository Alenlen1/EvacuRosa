NEXT_PUBLIC_API_URL=http://localhost:4000

# Required for /admin/login and /admin/dashboard — without these, the
# public map and evacuation-center listing still work fine.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
